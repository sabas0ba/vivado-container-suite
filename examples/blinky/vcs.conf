# Example project: an LED blinker for a 7-series board.
#
# Every key is documented in docs/02-configuration.md.  Machine-specific values
# (license server, Vivado location) belong in vcs.local.conf, which is
# git-ignored, or in the environment -- not here.

VCS_PROJECT_NAME=blinky

## --- device -----------------------------------------------------------------
# Digilent Arty A7-35T.  Change the part to match your board; the design itself
# is device independent.
VCS_PART=xc7a35ticsg324-1L
VCS_TOP=blinky

## --- sources ----------------------------------------------------------------
VCS_SOURCES=rtl/*.v
VCS_CONSTRAINTS=constr/*.xdc
VCS_GENERICS=CLK_HZ=100000000 BLINK_HZ=2

## --- simulation -------------------------------------------------------------
VCS_SIM_TOP=tb_blinky
VCS_SIM_SOURCES=sim/*.v
# The testbench overrides the divider so it finishes in microseconds.
VCS_SIM_TIME=20us

## --- output -----------------------------------------------------------------
VCS_BUILD_DIR=build
